apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
      # limits:
      #   cpu: 20Mi
      #   memory: 10Mi
      # requests:
      #   cpu: 20Mi
      #   memory: 10Mi
  restartPolicy: Always